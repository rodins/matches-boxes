<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="displayComponentHistory"
            type="com.sergeyrodin.matchesboxes.history.component.ComponentHistoryPresentation" />

        <variable
            name="clickListener"
            type="com.sergeyrodin.matchesboxes.history.component.ComponentHistoryPresentationClickListener" />
    </data>

    <LinearLayout
        android:id="@+id/layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:onClick="@{ () -> clickListener.onClick() }"
        app:highlightView="@{displayComponentHistory.isHighlighted}">

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/history_icon"
            app:srcCompat="@drawable/ic_history" />

<!--        Icons made by <a href="https://www.flaticon.com/authors/freepik" title="Freepik">Freepik</a> from <a href="https://www.flaticon.com/" title="Flaticon"> www.flaticon.com</a>-->

        <TextView
            android:id="@+id/name_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_gravity="center_vertical"
            android:text="@{displayComponentHistory.date}"
            android:textSize="@dimen/name_text_size"
            tools:text="середа лип.-29-2020 Time: 08:13" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{displayComponentHistory.quantity}"
            android:textSize="@dimen/quantity_text_size"
            tools:text="12" />

    </LinearLayout>
</layout>