<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="com.sergeyrodin.matchesboxes.component.addeditdelete.AddEditDeleteRadioComponentViewModel" />
    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".component.addeditdelete.AddEditDeleteRadioComponentFragment">

        <EditText
            android:id="@+id/component_edit"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="16dp"
            android:text="@{viewModel.name}"
            android:hint="@string/enter_component_name" />

    </FrameLayout>
</layout>